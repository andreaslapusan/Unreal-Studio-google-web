/**
 * /empleados — Login del portal Team. Usa el login UNIFICADO compartido.
 */
import React from 'react';
import PortalLogin from '../components/PortalLogin';

const EmpleadosLogin: React.FC = () => <PortalLogin portal="empleados" />;

export default EmpleadosLogin;

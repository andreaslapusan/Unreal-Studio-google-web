/** /admin/login — Login del portal Admin. Login UNIFICADO compartido. */
import React from 'react';
import PortalLogin from '../components/PortalLogin';

const AdminLogin: React.FC = () => <PortalLogin portal="admin" />;

export default AdminLogin;

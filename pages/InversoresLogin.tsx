/** /inversores/login — Login del portal Inversores. Login UNIFICADO compartido. */
import React from 'react';
import PortalLogin from '../components/PortalLogin';

const InversoresLogin: React.FC = () => <PortalLogin portal="inversores" />;

export default InversoresLogin;

/** /cliente/login — Login del portal Cliente. Login UNIFICADO compartido. */
import React from 'react';
import PortalLogin from '../components/PortalLogin';

const ClientLogin: React.FC = () => <PortalLogin portal="cliente" />;

export default ClientLogin;
